# Claude Code Console - Готовое решение для Docker + Portainer

## ✅ Текущее состояние

Контейнер запущен и Claude Code установлен. Теперь можно использовать.

## 🔐 Первая аутентификация

```bash
# Подключитесь к контейнеру
docker exec -it claude-code-console /bin/bash

# Выполните аутентификацию (один раз)
claude auth login

# Следуйте инструкциям в браузере
# После успешной аутентификации вернитесь в терминал

# Проверьте статус
claude auth status
```

## 🚀 Использование Claude Code

```bash
# Подключитесь к контейнеру (если еще не подключены)
docker exec -it claude-code-console /bin/bash

# Перейдите в рабочую директорию
cd ~/workspace

# Создайте проект (или работайте в существующем)
mkdir my-project
cd my-project

# Запустите Claude Code
claude

# Теперь можете общаться с Claude и давать задания
```

## 📦 Управление контейнером

### Через Portainer UI:
1. Откройте Portainer
2. Перейдите в `Containers`
3. Найдите `claude-code-console`
4. Используйте кнопки: Start / Stop / Restart
5. Для доступа к терминалу: нажмите `>_ Console` → выберите `/bin/bash` → `Connect`

### Через командную строку:

```bash
# Остановить контейнер
docker stop claude-code-console

# Запустить контейнер
docker start claude-code-console

# Перезапустить контейнер
docker restart claude-code-console

# Посмотреть логи
docker logs claude-code-console -f

# Подключиться к терминалу
docker exec -it claude-code-console /bin/bash
```

## 💾 Персистентные данные

Все важные данные сохраняются в Docker volumes:

- **claude-config**: Конфигурация и токены аутентификации
- **claude-workspace**: Ваши проекты и файлы

Даже после удаления и пересоздания контейнера данные сохранятся!

### Просмотр volumes в Portainer:
1. `Volumes` → найдите `claude-code-console_claude-config` и `claude-code-console_claude-workspace`
2. Можно просмотреть содержимое или создать резервную копию

### Резервное копирование через CLI:

```bash
# Создать бэкап конфигурации
docker run --rm \
  -v claude-code-console_claude-config:/data \
  -v $(pwd):/backup \
  ubuntu tar czf /backup/claude-config-backup.tar.gz -C /data .

# Создать бэкап workspace
docker run --rm \
  -v claude-code-console_claude-workspace:/data \
  -v $(pwd):/backup \
  ubuntu tar czf /backup/claude-workspace-backup.tar.gz -C /data .

# Восстановить из бэкапа
docker run --rm \
  -v claude-code-console_claude-config:/data \
  -v $(pwd):/backup \
  ubuntu tar xzf /backup/claude-config-backup.tar.gz -C /data
```

## 🔄 Обновление образа (если нужно пересобрать)

Если вы хотите пересобрать контейнер с обновленным Dockerfile:

```bash
cd ~/claude-docker

# Замените Dockerfile на обновленную версию

# Пересоберите образ
docker-compose down
docker-compose build --no-cache
docker-compose up -d

# Аутентификация сохранится в volume, повторять не нужно!
```

## 🛠️ Полезные команды Claude Code

```bash
# Справка
claude --help

# Версия
claude --version

# Проверка статуса аутентификации
claude auth status

# Выход из аутентификации (если нужно сменить аккаунт)
claude auth logout

# Запуск в конкретной директории
cd /path/to/project
claude

# Диагностика
claude doctor
```

## 🔧 Установка дополнительных инструментов в контейнер

```bash
# Подключитесь к контейнеру
docker exec -it claude-code-console /bin/bash

# Установите Node.js (если нужен)
sudo apt-get update
sudo apt-get install -y nodejs npm

# Установите другие инструменты
sudo apt-get install -y <package-name>

# Установленные пакеты сохранятся в контейнере,
# но при пересоздании образа их нужно будет установить заново
# Для постоянной установки добавьте в Dockerfile
```

## 📝 Примеры использования Claude Code

### Создание нового проекта:
```bash
cd ~/workspace
mkdir todo-app
cd todo-app
claude

# В Claude:
"Создай простое TODO приложение на Python с Flask"
```

### Анализ существующего кода:
```bash
cd ~/workspace/existing-project
claude

# В Claude:
"Проанализируй структуру этого проекта и предложи улучшения"
```

### Отладка:
```bash
claude

# В Claude:
"В файле app.py есть ошибка на строке 45, помоги исправить"
```

## 🌐 Доступ к контейнеру через SSH (опционально)

Если хотите подключаться к контейнеру через SSH:

1. Установите SSH сервер в контейнер
2. Пробросьте порт в docker-compose.yml:
```yaml
ports:
  - "2222:22"
```
3. Настройте SSH ключи

Но проще использовать `docker exec` или Portainer Console.

## ⚠️ Важные замечания

1. **Аутентификация сохраняется**: После первой аутентификации токены сохраняются в volume, повторять не нужно
2. **Workspace персистентен**: Все файлы в `~/workspace` сохраняются между перезапусками
3. **Обновления Claude Code**: Для обновления запустите внутри контейнера:
   ```bash
   curl -fsSL https://claude.ai/install.sh | bash
   ```

## 📚 Дополнительная информация

- [Документация Claude Code](https://code.claude.com/docs)
- [Документация Docker](https://docs.docker.com)
- [Документация Portainer](https://docs.portainer.io)

## 🎯 Быстрый старт (краткая версия)

```bash
# 1. Подключитесь к контейнеру
docker exec -it claude-code-console /bin/bash

# 2. Первая аутентификация (один раз)
claude auth login

# 3. Начните работу
cd ~/workspace
claude
```

Готово! Теперь у вас полностью рабочий Claude Code в Docker контейнере.
